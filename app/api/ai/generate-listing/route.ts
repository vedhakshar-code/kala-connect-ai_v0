import { NextRequest, NextResponse } from "next/server"; export async function POST(req: NextRequest) { try { const { craft, materials, laborHours } = await req.json(); const basePrice = Math.round(Number(laborHours) * 150 + 500); return NextResponse.json({ title: `Authentic Handmade ${craft || "Heritage Craft"}`, description: `Handcrafted using traditional ${materials || "natural materials"}. Crafted over ${laborHours || 12} hours of painstaking artisan labor, preserving generations of Indian cultural heritage.`, tags: [craft || "Craft", "Handmade", "Fair Trade", "GI Tagged"], price: basePrice }); } catch (err) { return NextResponse.json({ error: "Failed to generate AI listing" }, { status: 500 }); } }
