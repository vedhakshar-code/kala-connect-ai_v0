import type { Metadata } from "next";import { Inter } from "next/font/google";import "./globals.css";import { AppProvider } from "@/context/AppContext";import { StoreProvider } from "@/lib/store";import { LanguageProvider } from "@/context/LanguageContext";import { SiteHeader } from "@/components/site-header";import { SiteFooter } from "@/components/site-footer";const inter = Inter({ subsets: ["latin"] });export const metadata: Metadata = { title: "KalaConnect AI — Empowering Indian Artisans", description: "AI-powered marketplace connecting traditional artisans directly to global craft lovers." };export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) { return (<html lang="en" suppressHydrationWarning><body className={inter.className}><StoreProvider><AppProvider><LanguageProvider><div className="min-h-screen flex flex-col bg-[#FFF8E1]"><SiteHeader /><main className="flex-1">{children}</main><SiteFooter /></div></LanguageProvider></AppProvider></StoreProvider></body></html>);}
