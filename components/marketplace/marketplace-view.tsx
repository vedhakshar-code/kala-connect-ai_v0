"use client"; import React, { useState, useMemo } from "react"; import { ProductCard } from "@/components/product-card"; import { useAppContext } from "@/context/AppContext"; import { Search, Filter, X } from "lucide-react"; export default function MarketplaceView() { const { products } = useAppContext(); const [searchQuery, setSearchQuery] = useState(""); const [selectedCraft, setSelectedCraft] = useState<string | null>(null); const [selectedState, setSelectedState] = useState<string | null>(null); const [selectedPriceBand, setSelectedPriceBand] = useState<string | null>(null); const [sortBy, setSortBy] = useState<"featured" | "price-asc" | "price-desc" | "rating">("featured"); const crafts = useMemo(() => Array.from(new Set(products.map((p) => p.craft))), [products]); const states = useMemo(() => Array.from(new Set(products.map((p) => p.state))), [products]); const filteredProducts = useMemo(() => { return products.filter((p) => { const matchesSearch = p.title.toLowerCase().includes(searchQuery.toLowerCase()) || p.craft.toLowerCase().includes(searchQuery.toLowerCase()) || p.tags.some((t) => t.toLowerCase().includes(searchQuery.toLowerCase())); const matchesCraft = !selectedCraft || p.craft === selectedCraft; const matchesState = !selectedState || p.state === selectedState; let matchesPrice = true; if (selectedPriceBand === "under-2000") matchesPrice = p.price < 2000; else if (selectedPriceBand === "2000-5000") matchesPrice = p.price >= 2000 && p.price <= 5000; else if (selectedPriceBand === "above-5000") matchesPrice = p.price > 5000; return matchesSearch && matchesCraft && matchesState && matchesPrice; }).sort((a, b) => { if (sortBy === "price-asc") return a.price - b.price; if (sortBy === "price-desc") return b.price - a.price; if (sortBy === "rating") return b.rating - a.rating; return 0; }); }, [products, searchQuery, selectedCraft, selectedState, selectedPriceBand, sortBy]); return (<div className="max-w-7xl mx-auto px-4 py-8"><div className="flex flex-col md:flex-row gap-4 items-center justify-between mb-8"><div className="relative w-full md:w-96"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 w-4 h-4" /><input type="text" placeholder="Search crafts, artisans, or materials..." value={searchQuery} onChange={(e) => setSearchQuery(e.target.value)} className="w-full pl-10 pr-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-[#880E4F] bg-white text-sm" /></div><div className="flex items-center gap-3 w-full md:w-auto"><select value={sortBy} onChange={(e) => setSortBy(e.target.value as any)} className="px-3 py-2 border rounded-lg text-sm bg-white focus:outline-none focus:ring-2 focus:ring-[#880E4F]"><option value="featured">Featured</option><option value="price-asc">Price: Low to High</option><option value="price-desc">Price: High to Low</option><option value="rating">Top Rated</option></select></div></div><div className="grid grid-cols-1 md:grid-cols-4 gap-8"><div className="space-y-6"><div className="bg-white p-4 rounded-xl border border-gray-200"><h3 className="font-semibold text-[#880E4F] mb-3 text-sm uppercase tracking-wider">Craft</h3><div className="space-y-2">{crafts.map((c) => (<button key={c} onClick={() => setSelectedCraft(selectedCraft === c ? null : c)} className={`block w-full text-left text-sm px-2 py-1.5 rounded transition ${selectedCraft === c ? "bg-[#880E4F] text-white" : "hover:bg-gray-100 text-gray-700"}`}>{c}</button>))}</div></div></div><div className="md:col-span-3"><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">{filteredProducts.map((p) => (<ProductCard key={p.id} product={p} />))}</div></div></div></div>); }
